{
  "name": "notion-extension-proxy",
  "version": "1.0.0",
  "description": "Netlify functions to store data in Notion database",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
